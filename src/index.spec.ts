
test('an employee with no manager', () => {
  // display in the first column which represents management level 1
  // done
});

test('a manager who is not valid employee', () => {
  // display its manager id in the first column
  // display this employ in the second column
  // done
});

test('an employee with no name', () => {
  // display it as ID:[id]
  // done
});

test('an employee with no id', () => {
  // ignore it
  // done
});

test('two employees have same id and manager', () => {
  // display it under same manager without subordinates
  // done
});

test('two employees have same id but different managers', () => {
  // display it under different managers without subordinates
  // done
});

test('two employees have same id but different names', () => {
  // display it with different names
  // done
});

